# Devlogger Project

DevLogger is a MERN-style logging dashboard where developers can create and manage personal log entries tagged and classified by severity level. Users register and authenticate via JWT. Each log stores title, description, tags, and metadata, and only the owner can modify or delete it. The React frontend provides authenticated routing, contextual state for auth and logs, and a basic dashboard listing recent activity.
